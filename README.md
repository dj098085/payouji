# 狍游记 · 东北旅行应用（纯静态单页）

> 🦌 坨坨 & 果果陪你游东北 · **V1.3.6** · 四季通用

纯前端单页应用，数据存本机 localStorage，**零后端、零依赖、零注册登录**。
双击 `index.html` 即可离线运行；联网只是为了拉实时天气。

## 页面

| 文件 | 说明 |
|---|---|
| `index.html` | 应用本体（V1.3.6，含版本角标） |
| `plan.html` | 可上线规划方案 |
| `deploy.html` | 发布与备案路线说明 |

## 地域口径（重要）

**东北 ≠ 东三省。** 官方口径（国家发改委）= 黑吉辽三省 + **内蒙古自治区东部五盟市**（呼伦贝尔、兴安盟、通辽、赤峰、锡林郭勒），2007 年已纳入东北振兴战略实施范围。

应用内所有地域枚举（计价城市、酒店、进东北口岸、目的地、线路、美食、天气城市、停车）**均已覆盖蒙东**。行文一律用「黑吉辽 + 蒙东」，**不得使用「东北三省」指代东北**。

## 版本号规则（2026-09-20 定）

- **大版本**按整数叠加：`1.0 → 2.0 → 3.0`——功能结构级变化（新模块、架构调整、数据模型变更）
- **小版本**按 0.1 叠加：`1.0 → 1.1 → 1.2`——内容增补、话术优化、bug 修复、数据更新
- 版本号写在 `index.html` 的 `const APP_VER`，改这一个地方即可，页面右上角角标自动跟随
- 每次发版同步更新 `CHANGELOG.md` 并打 tag：小版本 `v1.1`，大版本 `v2.0`

## 功能一览（V1.0）

| 模块 | 内容 |
|---|---|
| 🎒 出发 | 四季切换（春/夏/秋/冬）联动装备、穿衣、线路、目的地、吃喝 |
| 🌤️ 实时天气 | 接 open-meteo 免费接口，东北 12 城实时温度，驱动角色换装换动作 |
| ✨ 规划 | 需求式智能规划：人数/天数/玩法/住宿偏好/预算 → 出完整行程与预算 |
| 🗺️ 行程 | 逐日行程、错峰五档期模型 |
| 🚗 自驾 | 三种方式、七档车型打分、九条线路、停车速查、成本五项拆算 |
| 💰 算价 | 按官方计价规则实时计算地铁/出租/网约车/住宿，含防宰红线 |
| 🔍 探索 | 小众目的地、本地小馆、20 条东北特产（角色举实物互动） |
| 🛠️ 工具 | 预算记账、打卡记录、清单、**分享链接 / 备份导入导出 / 二维码** |
| ⭐ 复盘 | 行后回顾与分享 |

## 无账号方案

**刻意不做注册登录**，原因：① 个人备案站点不得含会员注册；② 加账号要 +1–1.5 万 / +2 周；③ 首屏价值未被感知前设注册墙是典型流失点。
改用「分享链接 + 备份导入导出」解决换设备与发给同伴，零后端、零个人信息。
做微信小程序时用 `wx.login` 拿 openid 一键登录，几乎零成本。

## 当前线上地址

- 应用：https://paoyouji.app.workbuddy.host/ （国内实测 508–581ms 可达）
- 方案：https://paoyouji.app.workbuddy.host/plan.html
- 发布路线：https://paoyouji.app.workbuddy.host/deploy.html

## 代码仓库

- GitHub：https://github.com/dj098085/payouji （Public，只做版本管理与备份）
- 本仓库共 12 个 tag：`v1.0` → `v1.3.6`

GitHub 仓库**只做版本管理与备份** —— `*.github.io` 国内实测 226ms 快速失败，**不可作访问入口**。

### 推送命令

**github CLI 已授权**（账号 `dj098085`，令牌存系统 keyring，永不过期、无需手工输入）。

两个必须注意的点：**① 走代理**（直连 `github.com:443` 超时）；**② 在仓库目录内执行**（`git -C "中文路径"` 在本机会把路径编坏）。

```powershell
$gh = "C:\Program Files\GitHub CLI\gh.exe"      # 不在 PATH，要用全路径
$env:HTTPS_PROXY = "http://127.0.0.1:7897"
$t  = (& $gh auth token).Trim()                  # 从 keyring 取临时令牌
Set-Location "G:\我的AI短片素材库\东北IP\狍游记应用\repo"
git -c http.proxy=http://127.0.0.1:7897 -c credential.helper= `
    push "https://dj098085:$t@github.com/dj098085/payouji.git" main --tags
```

> `credential.helper=` 置空是刻意的：避免 git 去敲 keyring 而卡住。
> **不要加 `-u`** —— 它会把带令牌的 URL 明文写进 `.git/config`。

### 查看远端状态（不推送）

```powershell
& $gh api repos/dj098085/payouji/branches/main --jq ".commit.sha"
& $gh api repos/dj098085/payouji/tags --jq ".[].name"
```

## 合规红线

- 狍为国家二级保护动物：应用内不得出现猎具、陷阱、野味、皮草、买卖相关表述
- 卡通形象商用须先完成确权
- 价格数据一律来自人工审核库或公开计价规则，模型不得凭空估算
