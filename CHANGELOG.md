# 狍游记 · 版本记录

> 版本规则：大版本整数叠加（1.0 → 2.0），小版本 0.1 叠加（1.0 → 1.1）。
> 每次发版：改 `index.html` 里的 `const APP_VER` → 补本文件 → 打 tag（如 `v1.1.2`）→ 推送。

---

## V1.4.4 · 2026-09-21

**新增 PWA（可加到桌面、断网可打开）+ 天气离线三级降级兜底。已部署上线。**

### 背景

旅行场景会真的断网 —— 长白山、漠河、呼伦贝尔草原腹地都没有信号。原来的实现在
`fetchWeather` 失败时直接掉进 `SEASON_T` 兜底，那是**季节模拟值**，跟眼前天气毫无关系，
等于给用户看一个编出来的数字。

同时在手机上「添加到主屏幕」只能得到一个普通书签，图标是网页缩略图，打开还是浏览器外壳。

### 一、PWA 三件套

| 组件 | 文件 | 要点 |
|---|---|---|
| manifest | `publish/manifest.json` | `start_url: ./` / `scope: ./` / `display: standalone` / `orientation: portrait` |
| icons | `icon-192.png` `icon-512.png` `icon-maskable-512.png` `apple-touch-icon.png` | 从 `tt_cut.webp` 生成，底色 `#EFE3CE`，**maskable 安全区 78%** |
| Service Worker | `publish/sw.js` | 缓存 `paoyouji-v1.4.4` |

manifest 里还配了 2 个 `shortcuts`：长按桌面图标可直接跳「帮我规划」/「我的记录」。

### 二、Service Worker 缓存策略（刻意保守）

只保「打得开」，不做复杂的预缓存调度：

```
api.open-meteo.com    → stale-while-revalidate（miss 且无网时回 503 '{}'）
跨域请求               → 直接放行（保护二维码 api.qrserver.com）
req.mode === 'navigate' → network-first，失败回 './index.html'
其他静态资源           → cache-first
```

- `install`：**逐个 `add`**（单个失败不拖垮整体）+ `skipWaiting`
- `activate`：清掉非当前版本缓存 + `clients.claim`
- **只在 http/https 下注册** —— `file://` 打开时 SW 不被允许会抛异常
- 注册整体 `try` 住：**失败不影响任何功能**，只是没有离线能力
- 注册代码放在所有渲染之后，不阻塞首屏

### 三、天气离线三级降级（本次最有价值的设计）

```
成功          → 结构化结果存 localStorage
失败但有缓存   → 回放上次【真实】数据 + 标注「📦 离线，N 分钟前的数据」
失败且无缓存   → 才用季节模拟值（首次访问就断网）
```

- **超 6 小时（`WX_MAX_AGE`）才判过期**，避免无限期复用旧数据
- **来源标签从 3 档扩到 4 档**，如实标注不糊弄用户：

  | 标签 | 条件 |
  |---|---|
  | `我在这` | GPS 定位成功 |
  | `实时` | 手动选城市、接口实时返回 |
  | `📦 离线` | 回放缓存 |
  | `模拟` | 季节模拟值 |

- 缓存命中时同步补上「上次记录 x℃ ~ y℃　·　降水概率 z%」与预警抬头
- 坨坨会说明一句：「这儿没信号！先给你 XX N 分钟前的天气，走出去就自动更新。」

### 四、修掉一个微小但真实的观感问题

`wxAgo` 原来用 `Math.round`，导致 `round(30s / 60000) = 1` —— **「30 秒前」被显示成「1 分钟前」**。
改用 `Math.floor`，保证 59 秒内都是「刚刚」。7 个边界用例全部覆盖。

### 五、测试

| 项 | 结果 |
|---|---|
| 全量回归探针（**新建 `probe_v144.js`，18 节 46 项**） | **46/46 通过，0 运行期错误** |
| 分辨率 6 档（375 / 390 / 428 / 768 / 1280 / 1600） | **ALL PASS**，全无横向溢出、tab 全均分 |
| 三档断点 | 手机 480 / 平板 620 (83.1%) / 桌面 760 (60.4%, h1 36px) / 宽屏 900 (57.0%) |
| 缓存往返 + 过期判定 | `-12℃ 哈尔滨` 往返一致；7 小时前的记录正确判 `null` |
| `check_pwa.py` | **ALL OK** |
| 截图核验 | 12 张，144–220 KB，无空白 |
| **部署后回读** | 全部 200；`index.html` 334,105 B **与本地逐字节一致** |

新增探针第 18 节「响应式」的判据不是「源码里有没有 `@media`」，而是
**「`@media` 是否落在样式表末尾」** —— 直接对应 V1.4.3 那个「一半生效一半不生效」的坑。

**部署后回读 MIME 是关键**：`manifest.json` 返回 `application/json`、`sw.js` 返回
`text/javascript`。类型错了浏览器直接拒绝，PWA 完全不生效，但页面本身看起来正常，
**不查就发现不了**。

### 六、线上

**https://paoyouji.app.workbuddy.host/** （`APP_VER = 1.4.4`）

---

## V1.4.3 · 2026-09-21

**新增三档响应式大屏适配 + 无障碍 reduced-motion；统一旧导航文案。**

### 背景

V1.4.2 之后做的全量回归测试暴露了一个结构性短板：**全文 `@media` 数量 = 0**，
`#phone` 死锁在 `max-width:480px`，字号在任何屏幕都一样大。

实测（Chrome headless 五档）：

| 设备 | 修复前 phone 占屏 | 修复后 |
|---|---|---|
| iPhone14 (390) | 93.8% | 93.8%（不变） |
| iPad (768) | 64.3% | **83.1%** |
| Laptop (1280) | 38.2% | **60.4%** |
| Wide (1600) | — | **57.0%**（900px 上限） |

### 改动

| # | 内容 |
|---|---|
| 1 | `@media (min-width:720px)`：`#phone` → 620px，字号上调一档 |
| 2 | `@media (min-width:1080px)`：`#phone` → 760px + 两侧暖色渐变底 + 边框阴影，`.card` padding 17/19、`.hero h1` 36px、`h2.sec` 19px |
| 3 | `@media (min-width:1440px)`：`#phone` → 900px，`.grid2` 改三栏 |
| 4 | `@media (prefers-reduced-motion:reduce)`：关掉全部 animation / transition，隐藏 `.fall` 季节粒子 |
| 5 | 旧导航文案统一：`去"工具"页记几笔吧` → `去「我的 › 记录」记几笔吧`；`清单在「工具」页最后一栏` → `清单在「我的 › 记录」最下面那栏`；`统计到"复盘"页` → `统计到「我的 › 🌟复盘」` |

### 🔴 关键踩坑：媒体查询必须放在 `<style>` 末尾

**首版把 media query 写在了 `#phone` 附近（约 2180 行），实测完全无效** ——
`.card` / `h2.sec` / `.hero h1` 的基础规则在 7640 / 7966 行才定义，
CSS 同优先级按「后出现者胜」，媒体查询被基础规则原样盖了回去。

**症状**：`#phone` 宽度变了（因为它基础规则在前），但 `card-padding` 仍是
`14px 15px`、`h1` 仍是 `30px` —— 一半生效一半不生效，很容易误判成"选择器写错了"。

**对策**：所有 `@media` 一律追加到 `</style>` 之前（现为 403–448 行），并在注释里写明原因。

### 验证（全部实测）

| 项 | 结果 |
|---|---|
| 五档分辨率横向溢出 | 全部无 |
| tab 均分 | 全部 yes（96 / 124 / 152 / 180 px） |
| 最宽溢出元素 | 全部「无」 |
| 手机端行为 | 与改前**逐项一致**（480 / 14px 15px / 30px / 17px） |
| 桌面端字号 | `h1` 30→36px，`h2.sec` 17→19px |
| 桌面端内边距 | `14px 15px` → `17px 19px` |
| 逻辑回归 25 项 | **全通过**（语法 / 导航 / 段切换 / 天气 / 季节 / 衣着 / 消息 / 信用 / 存储） |

---

## V1.4.2 · 2026-09-21

**修复三个真 bug（全量回归测试发现）：复盘页无入口、v-partner 段切换失效、季节推断春夏误判。**

### 修法

| # | 问题 | 根因 | 修法 |
|---|---|---|---|
| 1 | **「复盘」页无入口** | `sub-review` 登记在 `SUB_OF['v-mine']` 里，但 `v-mine` 的 segbar 只有 记录/消息/信用 三个按钮，**没有 `data-sub="sub-review"`** → 页面永远不可达。而第 886 行还写着"进度自动统计到复盘页"，用户去找是找不到的 | segbar 补第 2 个按钮 `🌟 复盘`（记录 与 消息 之间） |
| 2 | **`v-partner` 段切换完全失效** | `SUB_OF` **漏登记 `v-partner` 键** → `showSub('v-partner')` 第一行 `if(!list.length) return` 直接早退，三个 subview 的显隐从不被管理 | `SUB_OF` 补 `'v-partner':['sub-partner-people','sub-partner-guide','sub-partner-rv']` |
| 3 | **季节推断 4–5 月判成秋** | `t>=14` 分支写死 `(m>=6&&m<=8)?'su':'au'`，5 月 16℃ 掉进 `'au'` | 重写：≥24℃ 恒夏；20–23℃ 在 4–5 月算春；15–19℃ 按 春/夏/秋 三分；6–8 月补 `'su'` 分支 |
| 4 | 衣着 `t<=14` 档写「薄羽绒」偏重 | 文案问题 | 改「风衣或薄外套，早晚偏凉加一件」 |

### 测试脚本自身也修了 3 处错

第一版探针报出的 4 个 false 里有 3 个是**测试写错了**，不是代码错：

- `wxBig` / `wxBadge` 是**不存在的 id**（实际叫 `wxTemp` / `wxTag`）
- `window.SUB_OF` 取不到 —— **顶层 `const` 不挂 `window`**，得用 `typeof` 直接探测词法作用域
- 扫 `outerHTML` 找 `data-v="v-tools"` 会**匹配到探针自己的源码字面量**，必须只扫真实 DOM 属性
- 衣着断言用 `8℃→含"羽绒"` 是错的用例（`8℃` 落在 `t<=14` 档），应为 `7℃`

### 教训

**测试报错时，先怀疑测试，再怀疑代码。** 4 个 false 里 3 个是探针自身缺陷——
若直接照着"修"，会把正确的代码改坏。

**新增检查项固化**：`SUB_OF` 是否覆盖全部多段 view、每个 subview 是否有对应 segbar 按钮、
`data-sub` 是否有悬挂引用 —— 这三个能自动拦住同类 bug。

---

## V1.4.1 · 2026-09-21

**修复：导航重整后遗留的旧 id 引用。**

### 问题

V1.4.0 把「工具」tab 的 section id 从 `v-tools` 改成了 `sub-record`，但 JS 里
**两处硬编码的 `[data-v="v-tools"]` 没跟着改**：

- `showInbox()` 里「我自己规划」按钮的跳转
- `shareBoot()` 里 `#gShare` 点击后的跳转

因为代码写的是 `if(tab) tab.click()`（找不到就静默跳过），**不会报错，但功能失效**——
用户点「生成分享链接」后页面不会滚到分享区。

### 修法

两处都改为 `[data-v="v-mine"]` + `showSub('v-mine','sub-record')`，
跳到「我的 > 记录」（分享模块现在在那儿）。

### 教训（已写入项目记忆）

**改 id 时必须全库搜一遍所有引用形式**，不能只改定义处。本次是线上部署后
用正则扫 `data-v="..."` 才发现的——**说明"部署后回读线上文件做结构校验"是必要步骤**。
静默失败（`if(el)` 守卫）比报错更危险，因为它不会在控制台留下痕迹。

---

## V1.4.0 · 2026-09-21

**导航重整（8→5）+ 天气逻辑修真（季节不再由月份硬套）+ 新增「我的」板块。**

### 一、导航重整：8 tab → 5 tab（需求③）

| 新 Tab | 收纳的旧 Tab | 二级切换 |
|---|---|---|
| ✨ 规划 | v-gen | 无 |
| 🗺️ 行程 | v-plan ＋ v-price ＋ v-drive | 线路 / 算价 / 自驾 |
| 🍄 探索 | v-explore ＋ v-depart | 目的地 / 行前 |
| 🐾 搭子 | **新增（V2 占位）** | 找搭子 / 本地通 / 房车 |
| 👤 我的 | v-tools ＋ v-review ＋ 消息 ＋ 信用 | 记录 / 消息 / 信用 |

**实现要点（零侵入）**
- 旧 section **保留原 id 与全部 DOM**，只把 `class="view"` 换成 `class="subview"`，
  内部所有事件绑定与渲染函数**一行未改**
- 新增 `SUB_OF` 映射表写死归属关系，不靠 DOM 顺序推断（顺序会变，MAP 不会）
- `.tabbar` 的 `flex:0 0 auto;min-width:52px`（原为 8 tab 横滚而设）**整段注释掉**，
  回到基础样式 `flex:1`，5 个 tab 实测各 **96px** 完美均分、无横向溢出

### 二、天气逻辑修正（用户重点反馈）

> 「像现在的页面标识的秋天，沈阳大连这些地方还穿着半截袖呢，真换成长袖的时候
> 可能要等到10月中下了，这段时间只是早晚有些凉并不是冷哈。」

**根因**：原代码 `m=new Date().getMonth()+1` 用**月份硬推季节**，9 月一律判成 `au`（秋），
于是 hero 直接给两小只套上秋装（`lv-chill` 围脖）。东北南北跨度太大（漠河与大连同期差 15℃+），
月份推季节必然出错。

**改法（三层解耦）**

| 层 | 依据 | 影响 |
|---|---|---|
| 衣着层 | **目的地实时气温** | 决定两小只穿什么，与季节标签**完全解耦** |
| 季节层 | 目的地实时气温反推（`seasonFromTemp`） | 只影响"去哪玩/吃什么"的内容推荐 |
| 定位层 | GPS 实测（新增「📍 用我的位置」） | 到达目的地后优先于手选城市 |

**关键阈值校准**：围脖档位从 `≤14℃` **下调到 `≤8℃`**。
原逻辑 14℃ 就戴围脖，导致 9 月沈阳（白天 25℃）仍显示秋装。新档位：
```
≥24℃ 短袖 ／ 20–23℃ 薄长袖 ／ 15–19℃ 长袖+薄外套
8–14℃ 围脖 ／ 0–7℃ 加手套 ／ ≤0℃ 耳包+大棉袄
```

**新增功能**
- 季节手动选择会**锁定**（`S.seasonLock=true`），不再被天气接口覆盖
- 今日概览：最高/最低温、降水概率、按**白天最高温**给出穿衣建议
- 恶劣天气提醒：雷雨 / 雨雪 / 阵风≥40km/h / 昼夜温差≥12℃
- 数据来源如实标注三档：`我在这`（GPS）/ `实时`（目的地）/ `模拟`（断网兜底）

### 三、新增「我的」板块

- **记录**：原 v-tools（记账/打卡/清单/分享）＋ v-review（复盘）合并
- **消息**（新）：组队申请 / 行程变更 / 搭子临时退出 / 系统通知，带未读小红点
- **信用档案**（新）：**只记客观事实，不做主观评价**

### ⚠️ 合规红线（写入代码注释，正式上线前必须复核）

1. **信用档案不得贴标签**：只记「约了几次、成行几次、距出发多久取消」，
   禁止「老赖」「骗子」「不靠谱」等主观评价，禁止用户互评分，**必须提供申诉更正入口**。
   理由：前者是事实陈述，后者构成名誉权侵权。
2. **「想认识伴侣」暂不开放**：婚恋交友在国内属强监管领域。
3. **本地通陪游有硬风险**：「私家车 + 收费载客 + 导游服务」同时触碰
   **非法营运**与**无证导游**两条线，页内已标注，需在正式做之前单独出合规方案。
4. **房车付费板块**需明确收款性质（会员费？撮合费？），个人无法直接收款。

### 技术验证（每改必验证）

| 项 | 结果 |
|---|---|
| 内联脚本语法 | ✅ OK（130 538 chars） |
| 导航 tab 数 | ✅ 5 |
| tab 均分宽度 | ✅ 96 / 96 / 96 / 96 / 96 |
| 横向溢出 | ✅ `bodyOverflow=false`（497=497） |
| 17 个 view/subview id | ✅ 全部存在 |
| 天气 13 个元素 | ✅ 全部存在 |
| 两小只容器 | ✅ 112×112 / 94×102（未变形） |
| JS 运行期错误 | ✅ 0 |
| 切换逻辑（点「我的」→ 点「消息」） | ✅ `v-mine:ON` → `sub-msg` 显示，3 条消息渲染 |

**踩坑记录**：Chrome headless 的 `--window-size=390` 实际渲染成 `innerWidth=512`，
截图却按 390 裁切 → **造成"右侧溢出"的假象**。实测量 `body.scrollWidth === clientWidth`
才是可靠判据。此坑已记入 `.workbuddy/memory/MEMORY.md`。

---

## V1.3.6 · 2026-09-21

**补上蒙东：东北不等于东三省。**

### 背景（用户诉求）
> 「东北并不是东三省哈，不要丢了蒙东」

### 口径依据
国家发改委《党领导东北地区振兴发展的历史经验与启示》：

> 东北地区包括东北三省（辽宁省、吉林省、黑龙江省）和内蒙古自治区东部五盟市
> （呼伦贝尔市、兴安盟、通辽市、赤峰市和锡林郭勒盟），总面积约 145 万平方千米。

该五盟市 2007 年已纳入国家东北振兴战略实施范围。此前应用的地域枚举（城市、计价、
酒店、目的地、线路、口岸）**一路只有黑吉辽**，蒙东全线缺失。

### 改动（22 处，全部为新增数据 + 少量文案）

**地域口径**
- hero `.sub` 明确写出「坨坨果果陪你游东北（黑吉辽 + 蒙东五盟市）」
- 天气台词（`SAY['lv-cool']` / `SAY['lv-mild']`）各补一句蒙东，让口径在交互里自然出现
  - 注：原打算加在 hero 台词 `#heroSay`，实测发现它会被 `applyTemp()` 用 `SAY[lv]`
    随机覆盖，会造成「闪一下再被换掉」，因此改走天气台词表

**计价城市（`CITY`）** 新增 3 市，均只在页面既有的「区间估计 / 以车内明码标价为准」声明下使用

| 城市 | 起步 | 里程 | 其他 |
|---|---|---|---|
| 呼伦贝尔（海拉尔） | 6 元 / 2km（夜间 6.8） | 2 元/km（夜间 2.4） | 15km 以上加收 50% 空驶费；低速等待 0.38 元/分钟 |
| 通辽 | 6 元 / 2.5km | 1.5 元/km，超 6km 按 2 元 | 等候 1 元/5 分钟；23:00–5:00 加收 20% |
| 赤峰 | 6 元 / 3km | 1.5 元/km，超 7km 按 2 元 | 22:00–5:00 加收 10% |

> 海拉尔来源：海拉尔区发改委 / 交通运输局公告；通辽来源：通辽市发改委与交通局
> 《取消燃气（油）附加费及调整运价标准的通知》；赤峰为公开报道口径。三者均按「估计」处理。

**其余数据表**
- `CITYIN` 加「海拉尔 / 满洲里进出」；同时修掉硬编码 `CITYIN[...*5]` → `CITYIN.length`
- `HOTEL` 加 `hlr`（呼伦贝尔/海拉尔，暑期档明显更高）与 `cf`（赤峰/通辽）
- `LODG` 加 `hlr` / `cf` —— **必须有**：规划器会用 `HOTEL[cityZ]` 与 `LODG[lz]`，
  新 hub 若漏挂这两张表会直接取到 undefined
- `WXCITY` 加 6 城：海拉尔、满洲里、乌兰浩特（兴安盟）、通辽、赤峰、锡林浩特
- `WANT` 加「🌾 草原牧场」+ `TAG_SEASON` 加 `grass:['su','au']` + `UNMET_TXT` 加对应说明
- `LINESET` 加 `grass` 线路名集合，避免蒙东模块被误判成「城市线」
- `MOD` 加 5 个模块（hub `hlr` / `cf`）：呼伦贝尔草原、满洲里、额尔古纳、阿尔山、克什克腾
- `DESTS` 加 5 条目的地
- `ROUTES` 四季各加 1 条：春·阿尔山杜鹃 / 夏·呼伦贝尔草原 / 秋·阿尔山秋色 / 冬·呼伦贝尔雪原
- `FOODS` 加 3 条：手把肉·烤羊腿 / 蒙式奶茶奶食 / 满洲里俄式西餐
- `DROUTES` 加 2 条自驾线：呼伦贝尔草原环线、阿尔山秋色
- `DVCITY` 加「海拉尔 / 满洲里取车」
- `PARK` 加 3 条停车指南
- `CITY.other` 的说明里补上乌兰浩特、锡林浩特

### 验证
- 全部补丁 22/22 命中（补丁脚本自带「任一处不命中就整体不写盘」的保护）
- JS 语法：`vm.Script` 编译首页整块 script（119,330 字符）通过
- 数据一致性：`CITYIN` 键 / `MOD.hub` ⊆ `HOTEL` ∪ `LODG` ∪ `CITY`，0 失败
- 真实渲染：Chrome headless 跑页面后 dump DOM，13/13 通过
  （`#appVer` 由 JS 填入 = 顶层 JS 跑通；蒙东目的地、天气城市、计价城市均出现在 DOM 中）

### 顺带修的口径问题
抖音运营策略两份文档里 3 处「东北三省」表述已改为黑吉辽口径（数据本身不变），
并在 DOU+ 定向建议里补上蒙东：抖音地域定向只能选到省，选「内蒙古」会连带蒙西，
可用**市级定向**（呼伦贝尔 / 通辽 / 赤峰）单独补。

---

## V1.3.5 · 2026-09-21

**把「东北话气泡」从屏幕底部挪进 hero 的 IP 形象区。**

### 背景（用户诉求）
> 「最好等到形象修改好之后再 IP 形象哪里以气泡的形式出现，不要出现在底部」

改前 `#neToast` 是 `position:fixed; left:50%; bottom:78px` —— 永远贴在屏幕底部，
和坨坨果果毫无关系。改后它成为 `.bears` 里的一个 flex 项，落在静态说明行 `.say`
之后，用 `::before` 三角尖角指向说话那只。

### 改动

**CSS**
- `#neToast`（fixed 定位在底部）→ `.ne-wrap`（flex item，`flex:1 1 100%`）
- 新增 `.ne-wrap::before` 尖角：`who-t` 指左（坨坨）、`who-g` 指右（果果）
  - 注意果果**不在容器最右**——它是紧跟在坨坨后面的第二个 flex 项，
    中心在 `112+12+47=171px`，所以尖角用 `left` 定位而不是 `right`
- 新增 `#bearBox.talking .say{display:none}`：气泡说话期间藏掉静态说明行，
  避免上下两个框叠着
- `.ne-txt` 补 `min-width:0`：否则 flex 子项的 `min-width:auto` 会撑破气泡右边界

**HTML**
- body 末尾的 `#neToast` 整块删除，改为插到 hero `.bears` 内 `.say` 之后

**JS**
- `neSay(txt, who, ms)` 增加 `who-t` / `who-g` 切换，并给 `#bearBox` 挂 `.talking`
- 新增 `neHide()`，超时与关闭按钮统一走它

### 一个刻意的取舍
气泡现在只在 hero 区出现。如果用户已经滚到页面下方，气泡会出现在看不到的地方——
它不会把页面强拉回顶部（那样更打扰）。所以长距离交互的主要反馈仍靠页面内的就地提示，
气泡只做气氛。

### 附：角色形象 3D 化（资产侧，本轮**未上线**）
本轮同时打通了 Blender 高精度角色管线（`旅行应用/blender/`），产出坨坨/果果的
毛发版五机位渲染图。**本轮不替换网页上的 2D 形象** —— 网页形象仍是
`tt_cut.webp` / `gg_cut.webp`，因为换成 3D 渲染图会让「按气温自动换围巾 / 耳包」
的 SVG 叠加层全部错位，需要单独一版来做对齐。对比板见
`blender/tt_M2_毛发版_对比板.png` 与 `blender/gg_M2_毛发版_对比板.png`。

---

## V1.3.4 · 2026-09-21

**移除坨坨/果果的全部动态效果，只保留直立静置。**

### 背景（用户诉求）
> 「先不要给狍坨坨和果果加效果，都去掉，只保持现在的直立状态，你现在做的跳起来的效果
> 就是图片给的抖动关键帧太僵硬了」

V1.3 上线的自动表演调度器每 7.8s 歪头、13.5s 挥手、26s 跳一下、42s 走过去拥抱，
其中「跳一下」（`.cute` 关键帧 `translateY(-6px) scale(1.07) rotate(±3deg)`）在写实照片上
是整体位移，没有关节配合，观感僵硬。**决定全部撤掉，回到纯静态直立。**

### 删了什么
**CSS（共 12 个区间）**
- 角色容器：`cursor:pointer`、`:hover{translateY(-5px)}`、`transition`（不再可点）
- `@keyframes` 全套：`waveHand` / `popUp` / `cute` / `tiltAct` / `hugWalkA` /
  `hugWalkB` / `hugHoldA` / `hugHoldB` / `hugBackA` / `hugBackB`
- 对应触发规则：`.bear-wrap.wave|.cute|.tilt|.hug*`、`.a-hand`、`.pops`、`.pop`、`.pop.hugtip`
- 常驻浮动：`.b-all` / `.bphoto` 上的 `animation:bob`；`.lv-frozen` 的 `shiver`（发抖）、
  `.lv-hot` 的 `bob 5.5s`；`.bear-wrap.holding` 的 `hvNod`
- 天气表情动画：`.e-breath`（哈气）、`.e-sweat`（汗滴）——连显示规则一起去掉，
  天气档位只影响**换装**，不再有动效
- `.gifpop` 4 条规则（GIF 表情气泡容器）

**HTML**
- 两个角色的 `.a-hand`（挥手手）、`.pops`（飘心容器）、`.gifpop`（GIF 气泡）共 6 个元素
- hero 提示文案由「👆 点一下卖个萌 · 双击我俩抱一个 · 还会跟着实时天气换装」
  改为「实时天气联动 · 天冷了自动换上围巾耳包」

**JS**
- 整个角色动作块：`bearTilt` / `bearWave` / `bearCute` / `bearHugTip` / `bearHug` /
  `bearSpawnPops` / `startBearActor`（含 4 条自动调度定时器 + 绑定在角色上的
  `click` / `dblclick` 监听）
- `gifpop` 点击逻辑、`GIFS` 常量（顺带去掉 6 个外部 GIF 依赖）、`petTpl()` 里的 `.gifpop` 模板

**顺带清理**：CSS 里残留 9 行 keyframes 残片（`50%{transform:...}}` 这类无头无尾的片段，
历史编辑事故遗留，浏览器会整段丢弃），本次一并删除；同时删掉已失效的
`.bear-wrap.holding .b-arm.aR`（旧 SVG 部件规则）。

### 保留了什么
- **直立静置的角色本体**（透明 WebP，V1.3.3 抠图）
- **实时天气换装**：`.lv-chill/freeze/frozen` 显示围巾 / 耳包 / 手套 / 外套，`.lv-warm/hot` 显示墨镜
- **配色皮肤**（`data-skin` hue-rotate）、**换形象面板**、**举实物**（`setHold`）
- **落叶氛围**（`.fall`，属页面季节氛围，与角色无关）
- 东北话气泡 (`neSay`)、四季切换、价格引擎、记账、导出导入、分享等全部业务功能

共删除 246 行；`APP_VER` → 1.3.4，tag `v1.3.4`。

### 验证
1. **运行时诊断**（注入脚本读 `document.getAnimations()`，跑满 50s 虚拟时间覆盖原来
   全部调度周期）：`BEAR_ANIM=0`，页面上仅剩 14 个 `fall @ SPAN`（落叶）；
   `.a-hand / .pops / .gifpop` 各 0 个；`.gear / .hold-pop / .bear-wrap` 各 5 个（保留）。
2. **像素比对**：6s 与 50s 两个时刻截图，角色区 98.87% 像素完全相同，
   剩余 1.01% 差异经差异图确认**全部是飘过的落叶**——角色本体零位移。
3. `node --check` 通过；Chrome 截图确认 hero 版式、装扮层定位、气泡、形象面板均正常。

### 已知未处理（留档待定）
- **`.hold-pop` 没有任何 CSS 定义**（"举实物"的浮层容器），`showFood()` 触发后
  实物图会以块级元素插在角色下方并撑开 hero 版式。属 V1.3.3 之前就存在的坏点，
  与本次改动无关，未擅自修改，待确认后再处理。
- `@keyframes bob/shiver/puff/sweatdrop/hvNod` 的定义在本版之前**就已经丢失**
  （被历史编辑切成残片），即那些 `animation:` 引用早已失效——这也是为什么
  真正在跑的只有 4 条自动调度。

---

## V1.3.3 · 2026-09-21

**修复：果果被抠掉左耳/左臂（「缺肉」）；去掉角色名字牌。**

### 问题（两条用户反馈）
1. **「不要加坨坨和果果的字」**——V1.3 换写实照片后，在角色下方加了一枚白色圆角名字牌
   （`<span class="btag">坨坨</span>` / `果果`），属多余装饰。
2. **「果果都被你扣的缺肉了」**——V1.3.2 的抠图把果果**整只左耳切掉**，左脸颊被削平、左前肢缺失。
   按真实显示尺寸放大后一眼可见（坨坨也有轻微同侧削边）。

### 根因
V1.3.2 抠图脚本里有一条**「左侧打光柔晕清理」**规则：
```python
z2[int(h*0.03):int(h*0.78), :int(w*0.42)] = True          # 左侧 42% 宽、上部 78% 高的矩形
grown, n = grow_limited(bgm, (d < 52) & z2, 10)            # 从背景向矩形内扩散 10 圈
```
果果的**左耳、左脸颊、左前肢恰好都长在这个矩形里**，且毛色与背景的距离同样落在 13–52
（暖奶油白 vs 米色背景，最近处仅 9），于是被这条规则整块吞掉。

### 修复
- 抠图脚本升级为 **v6**（`tools/cutout_character.py`）：
  - **删除柔晕清理规则**——柔晕极淡、肉眼几乎不可见，**保角色身份优先，宁可留柔晕**
  - 底部参数保守化：清理带起点 `y > 0.86h`（原 0.835）、蹄核阈值 100 → 110、外扩 8px → 6px
  - 删除**主体连通域清理**（它会把贴边部件误删，也是此前「白环」的来源）
- 页面侧删除 3 处名字牌（`坨坨` / `果果` / `自定义`），同步清掉 `.btag` 的两条 CSS
  与 `petTpl()` 里那条已失效的 `replace` 正则

### 验证
- **逐像素审计**（把 `d>40` 的实体前景与抠图 alpha 相与）：
  - 坨坨丢失 2 240 px（3.48%）、果果丢失 2 301 px（5.47%）
  - **丢失区域全部是脚底阴影**，鹿角/耳朵/脸/手臂/蹄子**零缺失**（可视化见提交附件）
- 透明占比：坨坨 67.0%、果果 69.3%（V1.3.2 为 67.5% / **74.2%**——果果多切了 4.9% 的面积）
- Chrome headless 截图：静置 / 拥抱 / 极寒换装三态正常，版式与落叶不受影响，围巾耳包定位正确
- `node --check` 通过 `<script>` 语法校验

### 未采纳的方案（留档，别再试）
- **边缘感知连通填充**（低对比 + 低梯度从画布边缘渗透）：会留一圈**白环**，正是此前被投诉的现象
- **二维多项式背景建模**：拟合不出母版的渐晕，边缘残差 2.5–7.5 灰阶
- **分块中值 + 扩散补全背景**：会把果果的肚皮打穿
- **纯颜色阈值（TOL 5–13 扫描）**：阴影与浅色肚皮在颜色上不可分，必然误伤

---

## V1.3.2 · 2026-09-20

**修复：脚底残留的浅色阴影块，角色图改为内联（不再依赖外部托管）。**

### 问题
V1.3.1 抠完图后，角色脚底仍残留一块**浅色团块**（不是深色阴影，而是接近背景色的偏亮块），
在页面上看就是「角色底下跟着一块东西」。按 112px 的真实显示尺寸放大 9 倍可稳定复现。

### 根因
1. V1.3.1 的底部规则是 `dilate(强蹄像素, 16px)`——**外扩 16px 把脚掌周围一圈阴影全部保了下来**，
   脚底正下方约 14px 高、150px 宽的浅色区域整块留存
2. 母版的**背景本身带上下渐晕**（下缘比上缘暗约 30–40 灰阶），这部分渐变超出全局背景容差，
   被当作前景保留（这也是「阴影」的很大一部分来源）
3. 底部阴影与角色浅色肚皮在颜色上**不可分**（两者到背景的距离都落在 20–90，直方图连续无峰）

### 修复
- 抠图脚本升级为 v5（归档 `tools/cutout_character.py`）：
  - 底部清理带起点下移到 `y > 0.835h`（**肚皮已结束**的高度），带内只保留「蹄核 + 外扩 8px」
  - 追加**逐列裁到蹄底**规则，去掉蹄子正下方那条阴影裙边
  - 追加**主体连通域清理**，丢弃被腿围出来的孤立浅色口袋
  - 果果「左侧打光柔晕」清理保留
- 角色图改为**内联 data URI**（WebP base64，共约 56KB）：首屏关键元素不再依赖外部静态托管，
  彻底排除「托管侧图丢了/换域名导致角色不见」的风险

### 验证
- 按页面真实显示尺寸（坨坨 112px / 果果 94px）合成对比：V1.3.1 脚下有明显浅色块，V1.3.2 干净
- Chrome headless 截图：常温态 / 拥抱态版式正常，两个角色完整无缺件
- 内联后 `node --check` 通过 `<script>` 语法校验
- 透明占比：坨坨 67.5% / 果果 74.2%

### 未采纳的方案（留档，别再试）
- **逐级升温扩散（TOL 14→62）**：穿透轮廓，啃掉坨坨左脸、果果左耳与左臂
- **边缘感知连通填充（低对比 + 低梯度从画布边缘渗透）**：T=110/G=3.0 能让坨坨变干净，
  但会在角色外围留一圈半透明**白环**（正是 V1.2 被投诉过的问题），果果还会被啃
- **二维多项式背景建模**：2–3 阶拟合不出渐晕，残差 2.5–7.5 灰阶，外围留大片假前景
- **分块中值 + 扩散补全背景模型**：坨坨效果好，但果果身体颜色太接近背景，
  模型被拉偏，肚皮被打穿
- **只按颜色阈值切阴影/脚掌**：直方图连续无峰，理论上不可分

### 教训
> 抠图的难点不在「背景」，在**背景本身不均匀**（渐晕）+ **角色颜色贴近背景**（奶油白）。
> 这类图不要指望单一颜色阈值；先用「边框连通」锁定背景，再用**空间规则**（限定高度带、限定矩形）
> 清残留，并且**每一步都要按真实显示尺寸复核**——480px 图上 10px 的残留，在 112px 显示时约 2.3px，
> 9 倍放大才看得见，凭感觉会漏判。

---

## V1.3.1 · 2026-09-20

**修复：角色干净脱离背景（透明抠图）。**

### 问题
角色动的时候会连带一块方形/蛋形底板一起动，脚下还有两块影子，看起来不像独立人物。

### 根因（三处叠加）
1. `.bphoto` 容器带 `border-radius:46% 46% 38% 38%` + `overflow:hidden` + `background:#F5E6D0`——把方形母版图裁成蛋形并垫了一块米色底，与 hero 渐变背景形成可见轮廓；加上 `bob` 动画就成了「一块图层在浮动」
2. 上版为表现重量感加的 `.bear-wrap:after` 椭圆投影，每个角色一块，常驻显示，与母版自带阴影叠成「两块影子」
3. 母版图是**方形纯色底无透明通道**（背景 RGB 253,236,208），本身就带地面阴影与左侧打光柔晕

### 修复
- **抠图**：新增 `_cutout3.py`。严格颜色容差 + 从图像边框出发的**连通扩散**判定背景（果果奶油白毛与背景色距离仅 9，纯颜色规则会误伤，必须用连通性保护）；底部阴影按「脚掌核心 16px 范围」逐区清理；果果左侧柔晕在限定矩形内做 10 圈受限扩散（防止穿透轮廓）。输出 480×480 透明 WebP，各 24KB / 21KB
- **拆底板**：`.bphoto` 去掉 `border-radius` / `overflow:hidden` / `background`
- **删投影**：移除 `.bear-wrap:after`、`.bear-wrap.hugHoldA/B:after` 与 `@keyframes shLift`
- 保持图片与旧母版**同比例不裁切**——裁紧会让角色在图中占比变化，导致 `.gear` 装扮层错位（围巾戴到下巴上，已实测踩坑）

### 未采纳的方案（留档）
- 逐级升温测地扩散（TOL 14→62）：会**穿透角色轮廓**，实测啃掉坨坨左脸、果果左耳与左臂，已否定
- 颜色阈值切分阴影/脚掌：直方图连续无峰，无法切分
- 裁紧图片：破坏 `.gear` 装扮定位

---

## V1.0 · 2026-09-20

**首个正式版本。** 从「国庆限定」转为**四季通用**后的完整形态，功能结构定稿。

### 核心
- **四季为一级维度**：春 4–5 / 夏 6–8 / 秋 9–10 / 冬 11–3，切换即联动穿衣、装备、线路、目的地、吃喝、错峰档期
- **需求式智能规划**：人数 / 天数 / 11 项玩法多选 / 住宿偏好 / 预算 → 22 个玩法模块打分 + 地理线路约束 → 出逐日行程、住宿建议、交通方案、预算五项分表
- **季节不匹配时的「将就版」**：夏季想看雪→长白山高山残雪；冬季想看海→大连海滨冬景，用东北话把「为什么将就」讲明白

### 模块
- 🎒 出发：四季装备清单、穿衣建议、五档期错峰模型（周末/五一/暑期/十一/冰雪季）
- 🌤️ 实时天气：open-meteo 免费接口，东北 12 城，驱动角色换装换动作（七档温度）
- 🚗 自驾：三种方式（自带车/落地租车/落地包车）、七档车型打分推荐、九条线路、10 处停车速查、成本五项拆算、雪地胎辟谣专区
- 💰 算价：按官方计价规则实时计算（地铁里程分段票价表 / 出租车运价公式 / 网约车 / 机场巴士），住宿按档次×档期溢价，给防宰红线
- 🔍 探索：16 个小众目的地、本地小馆、**20 条东北特产**（点一下坨坨果果举起来给你看）
- 🛠️ 工具：预算记账、打卡记录、清单、**分享链接 / 备份导入导出 / 二维码**
- ⭐ 复盘：行后回顾

### 趣味化
- **分层 SVG 角色**：照优化母版手绘，呼吸/眨眼/摇尾/耳抖/招手，按温度换装（≤−15℃ 发抖哈白气，≥26℃ 墨镜冒汗）
- **东北话人格化**：分时段问候、每 52–100 秒随机冒话（你瞅啥/贼拉冷/得劲儿…）、报错全部用调皮友好的东北话
- **电子宠物定制**：左右角色位可换形象 + 五套配色，支持贴自定义图片链接
- **开放素材库**：特产与角色均有 `img` 字段，界面内贴链接即生效

### 微信分享
- og:title / og:description / og:image 已配，微信转发显示标题 + 狍坨坨缩略图
- 页内提示「点右上角 ··· → 发送给朋友」，并提醒微信内置浏览器不支持下载文件

### 关键决策
- **不做注册登录**：改用分享链接 + 备份导入导出（详见 README）
- **发布托管**：WorkBuddy 免费托管为唯一访问入口；GitHub 仅作版本备份
- **暂不办理 ICP 备案**：终态是微信小程序（走小程序备案），个人备案站点不得商业化

### 已知待办
- 地级市（牡丹江、延吉、丹东等）出租车运价为区间估计，需补齐各地发改委运价文件
- 关键帧/素材：特产与形象图片待补真实素材（开放素材库已留接口）
- 可用性测试：需 5 人实测（2 组结伴 + 2 对情侣 + 1 个东北本地人）

---

## v1.1 · 2026-09-20

- **视觉**：Hero 区角色恢复为 3D 毛绒优化母版图（狍坨坨 / 狍果果），保留天气驱动的围巾 / 耳包 / 手套 / 棉袄 / 墨镜 / 汗滴 / 哈气叠加动效
- **修复**：果果蓝靛果枝头饰回归官方设定位置（右耳上方）
- **微信分享卡片缩略图**同步更换为高清晰版狍坨坨母版

---

## v1.1.1 · 2026-09-20

- **修复**：删除旧版圆形头像 CSS 残留的 `border:3px solid #fff`，消除角色身后白色圆环

---

## v1.3.0 · 2026-09-20

### 修复：写实照片上盖眼皮导致的恐怖谷
- **移除** `.eyelids` / `.eyelid` / `.blush` 三层叠加（原做法是在 3D 照片的眼睛位置覆盖深棕色椭圆做 `scaleY(0→1)`，凭空冒出两块色块，观感诡异）。
- **替换为「歪头」**：整体绕脚底旋转 −7° 静置后回正，配合轻微重心起伏。用姿态而非五官做表情，不触碰照片上的写实五官。

### 重做：两小只的互动，从「平移撞一下」升级为「走过去抱住」
- 旧 `nudgeR/nudgeL` 只是 `translateX(±24px)`，本质是整张图平移，没有身体动作，已删除。
- 新三段式编排 `bearHug()`：
  1. **走过去**（1.15s）：绕脚底 `transform-origin:50% 88%` 左右交替倾斜 ±6.5° + 重心起伏，形成踏步迈腿感；两小只反相摆动（一个迈左脚一个迈右脚）。
  2. **扑上去抱住**（1.75s）：各自向内侧倾斜 13–16°、起跳抬升 11px、`scale(1.07,.92)` 贴身挤压，并在下沉/上抬之间做两拍弹动；头顶升起 💕💖 粒子与「抱一个／蹭蹭～」气泡。
  3. **松开走回**（1.0s）：反相踏步退回原位。
- 新增**地面投影** `.bear-wrap:after`（径向渐变椭圆），拥抱起跳时随之缩小变淡 → 给「起跳」提供落点参照，动作才有重量感。

### 其他
- 顺手修正 `.bear-wrap.b1 svg{width:112px;height:124px}` 会误伤 `.a-hand` 手掌尺寸的问题（旧规则特异性高于 `.a-hand`，会把 30px 的手掌撑成 112×124）。
- **单击角色＝卖萌，双击角色＝立刻抱一个**（不必等 42s 随机轮播），页内提示已同步更新。
- 动作调度改为 `tilt` 7.8s / `wave` 13.5s / `cute` 26s / `hug` 42s 随机触发；`hug` 期间独占锁 4.4s，避免动作叠加。
- 全部动效走 `transform` / `opacity`，元素绝对定位在 `.bear-wrap` 内，**不占文档流** —— 截图复核规划器、季节选择器、落叶粒子、底部页签版式均未受影响。

## v1.2.0 · 2026-09-20

- **3D 毛绒角色小动作**：在保留 3D 照片质感的前提下，通过叠加层实现眨眼、挥手、卖萌脸红、头顶爱心/星星粒子、两小只偶尔打闹撞一撞
  - 眨眼：眼睑为深棕半透明椭圆，定时闭合，不影响照片本身
  - 挥手：右侧伸出卡通手掌 SVG，定时挥手
  - 卖萌：身体放大摇摆 + 腮红 + 头顶爱心/星星
  - 打闹：坨坨果果互相靠近碰撞，伴随 `💥` / `💕` 粒子
  - 点击角色也可触发卖萌互动
- **版式安全**：所有动画均使用 `transform` / `opacity`，叠加层绝对定位在 `.bear-wrap` 内部，不占用文档流，不破坏下方版式
- **版本号**：`1.1` → `1.2`

---

## v1.1.2 · 2026-09-20

- **修复**：恢复 v1.1 替换 3D 母版时误删的大量 CSS，包括规划器选项、自驾模块、美食模块、形象定制、天气卡片、分享结果、清单工具、落叶粒子等样式
- **验证**：DOM 渲染后 `.opt`、`.fall`、`.bphoto`、`.ne-bub`、`.droute`、`.fd` 等关键类均已生效
